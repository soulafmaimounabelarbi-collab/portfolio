# Deployment task TODO

- [x] Update `vite.config.js` to support GitHub Pages/Vercel sub-path (set `base` via `process.env.BASE_URL` or default to `/`).
- [x] Restore `package.json` and add deploy-related scripts for GitHub Pages (`gh-pages`).
- [x] Add `gh-pages` devDependency.
- [x] Add `.env.example` (optional) for `BASE_URL`.

- [x] Add Vercel-friendly SPA handling note (no file changes unless needed).

- [x] Install deps and run `npm run build` + `npm run preview` to verify assets.
- [x] Run `npm run deploy` to GitHub Pages and verify `/dashboard` navigation + hard refresh.




